language: node_js
node_js:
  - "stable"
branches:
  only:
    - travis
install:
  - npm install
script:
  - npm test
cache:
  -npm install
services:
  - postgresql
before_script:
  - createdb postit_test --username=postgres
  - sequelize db:migrate
