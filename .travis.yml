language: node_js
node_js:
  - "stable"
branches:
  only:
    - travis
install:
  - npm install
script:
  - npm test
cache:
  -npm install
services:
  - postgresql
before_script:
  - psql -c 'create database postit-dev' -U postgres
  - sequelize db:migrate
